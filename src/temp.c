// while (stack_len > 3)                        
	// {
	// 	if (stack_len > res) // 75
	// 	{
	// 		if ((*stack_a)->i <= (stack_len - res)) //25
	// 		{
	// 			pb(stack_a, stack_b);
	// 			stack_len--;
	// 		}
	// 		else
	// 			ra(stack_a);
	// 	}
	// 	else
	// 	{
	// 		// len -= quarter;
	// 		// quarter = len - quarter;
	// 		res -= quarter; //50 //25 //0
	// 		set_idx(stack_len, *stack_a);
	// 	}
	// }
	// int		len;

	// // len = ft_stacklen(*stack_a);
	// if (ft_stacklen(*stack_a) <= 3)
	// 	return ;
	// set_idx(ft_stacklen(*stack_a), *stack_a);
	// // print_stack(*stack_a);
	// len = ft_stacklen(*stack_a) / 2;
	// while (ft_stacklen((*stack_a)) != len && ft_stacklen(*stack_a) > 3)
	// {
	// 	if ((*stack_a)->i <= len)
	// 		pb(stack_a, stack_b);
	// 	else
	// 		ra(stack_a);
	// }
	// return (quick_sort(stack_a, stack_b));
	// int		pivot;

	// set_idx(ft_stacklen(*stack_a), *stack_a);
	// print_stack(*stack_a);
	// if (ft_stacklen(*stack_a) <= 3)
	// {
	// 	// if (!check_sort(stack_a))
	// 	// 	sort3(stack_a);
	// 	return ;
	// }
	// ft_printf("len = %d\n", ft_stacklen(*stack_a));
	// pivot = ft_stacklen(*stack_a) / 2;
	// ft_printf("pivot = %d\n", pivot);
	// while (ft_stacklen(*stack_a) != pivot && ft_stacklen(*stack_a) > 3)
	// {
	// 	if ((*stack_a)->i <= pivot)
	// 		pb(stack_a, stack_b);
	// 	else
	// 		ra(stack_a);
	// }
	// print_stack(*stack_a);
	// return (quick_sort(stack_a, stack_b));